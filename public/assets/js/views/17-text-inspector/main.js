import textInspector from"../../../js/modules/textInspector.js";import htmlEntities from"../../../js/modules/htmlEntities.js";const textOutput=document.getElementById("textOutput"),textAreaInput=document.getElementById("textAreaInput"),demoBtn=document.getElementById("demoBtn"),resetBtn=document.getElementById("resetBtn");function handleTextInspector(t){let e=textInspector(htmlEntities(t)),n="";for(let t in e.wordDistribution)if(e.wordDistribution.hasOwnProperty(t)){n+=`<span><strong>Word:</strong> ${t} <strong>=</strong> ${e.wordDistribution[t]}</span>`}textOutput.innerHTML=`\n        <p>Characters: <strong>${e.characters}</strong></p>\n        <p>Words: <strong>${e.words}</strong></p>\n        <p>Lines: <strong>${e.lines}</strong></p>\n        <p>Word Distribution:</p>\n        <pre>${n}</pre>\n    `}handleTextInspector(textAreaInput.value),textAreaInput.addEventListener("input",(t=>handleTextInspector(t.currentTarget.value))),demoBtn.addEventListener("click",(t=>{t.preventDefault(),textAreaInput.value="Hello World this is a example content",handleTextInspector(textAreaInput.value)}),!1),resetBtn.addEventListener("click",(t=>{t.preventDefault(),textAreaInput.value=""}),!1);